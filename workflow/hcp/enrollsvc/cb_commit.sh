#!/bin/bash

# See the comment in cb_checkout.sh for more detail. Enrollment has occurred, in the
# directory chosen by cb_checkout.sh and passed to us in $2 ($outdir).
#
# Invocation;
#   $COMMIT "$ekhash" "$outdir" "$hostname" "$DBDIR" "$CONF"
# Stdout is ignored.

